apiVersion: image.toolkit.fluxcd.io/v1alpha1
kind: ImagePolicy
metadata:
  name: stagingimageupdate
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: stagingimageupdate
  policy:
    semver:
      range: '>= 1.0.0 <2.0.0'